# Setup — 기획 문서

## 프로젝트 개요

- **프로젝트명**: Setup
- **주제**: 청년층(대학생·취준생·사회초년생)을 위한 AI 기반 금융 자립 & 지출 케어 내비게이션
- **팀 규모**: 6인 / **기간**: 5주
- **한 문장 정의**: 청년의 과거 지출을 미래 자산 궤적으로 번역하고, 그 궤적에서 원인을 설명하며, 사용자 스스로 시나리오를 비교해 행동을 선택하게 만든다.

---

## 1. 설득력 설계도: A → B → C → D+E

| 연결 | 무엇 | 왜 |
|---|---|---|
| **A 문제** | 청년은 지출 리포트를 봐도 미래 결과를 몰라 행동이 안 바뀌고, 받을 수 있는 정책도 자산 형성으로 못 넘어감 | 지식 부족이 아니라 **행동 연결 실패**가 근본 원인 |
| **B 솔루션** | 소비·소득·정책 자격을 **하나의 미래 자산 궤적**으로 통합하는 AI 코파일럿 | 기존 앱 전부 회고형(retrospective) → 미래형(prospective) 시나리오 비교가 시장 공백 |
| **C 기능** | 인과 서사 생성 + baseline 궤적 + 정책 결합 + 시나리오 delta + 사용자 if-then 계획 | 추천("줄이세요")이 아니라 이해("왜 그렇게 됐고 바꾸면 얼마 달라지는가") → 행동과학 입증된 유일한 경로 |
| **D+E 기술** | Pandas 집계 + numpy-financial FV + LLM structured output + IsolationForest | 결정론적 계산으로 해석 가능성 확보, LLM은 사실 서술로 제한 → hallucination 없이 5주 내 구현 |

---

## 2. 문제 정의

### 2-1. 정량 근거

| # | 문제 신호 | 데이터 | 출처 | 등급 |
|---|---|---|---|---|
| P1 | 지식이 아니라 행동이 약함 | 20대 금융이해력 62.6점 < 성인 평균 65.7점, 금융행위·재무관리 영역 특히 저조 | [한국은행 2024](https://www.bok.or.kr/portal/bbs/B0000502/view.do?menuNo=201265&nttId=10091152) | A |
| P2 | 소액 반복 소비 누적 | 20대 월평균 구독 44,428원(연환산 53만), OTT 이용률 90.1% | [서울시 실태조사(etnews)](https://www.etnews.com/20250416000319) | B |
| P3 | 외식·배달 절감 1순위 | 2026년 소비 부담 1순위 카테고리, 가장 먼저 줄이려는 항목 | [트렌드모니터](https://www.trendmonitor.co.kr/tmweb/trend/allTrend/detail.do?bIdx=3319&code=0401&trendType=CKOREA) | C |
| P4 | 소득 불규칙 | 알바·취준생 고정월예산 깨짐; 불규칙 소득 관리 질문 다수; 월급루틴 콘텐츠 조회수 42만(E) | 커뮤니티(a-ha.io), YouTube(E) | E |
| P5 | 정책-자산 분리 | 도약계좌 대상 600만 중 가입 33.4%, 가입자 중 중도해지 16% | [연합뉴스](https://www.yna.co.kr/view/AKR20250524041900002), [파이낸셜포스트](https://www.financialpost.co.kr/news/articleView.html?idxno=231667) | B/C |

### 2-2. 정성 근거 (커뮤니티·SNS)

| 패턴 | 근거 | 함의 |
|---|---|---|
| 가계부 이탈 = 수기 입력 + 분류 오류 + 유료 게이트 | 뱅크샐러드 공식 가이드(B), 편한가계부 스토어(C), 블로그(E) | 자동 수집 필수, 오류 수정 최소화 |
| "받자마자 선분배"가 검증된 멘탈 모델 | 링커리어 꿀팁(E), 브런치 에세이(E) | 버킷 UX 골격 커뮤니티 검증 |
| 구독 피로·무지출 챌린지 대중화 | JTBC ~479K views(E), 구독 리포트(E) | 참여형 게이미피케이션 수요 |
| 월급루틴(자동배분) 포맷 인기 | YouTube ~421K views(E) | 자동 분배 + 시각화 피드백 유효 |

### 2-3. 종합 진단

> 청년에게 부족한 것은 정보가 아니라 **연결**이다.
> 지출 리포트(P1-P3)와 정책 안내(P5)가 따로 놀고,
> 소득이 불규칙해서(P4) 고정 예산이 깨지며,
> 소액 반복 소비(P2)는 눈에 안 보인다.
> 이 세 갈래가 하나의 화면에서 만난 적이 없다.

---

## 3. 기존 솔루션 공백

| 서비스 | 하는 것 | 못 하는 것 |
|---|---|---|
| 토스 | 잔액·소비 자동 조회, 마이데이터 분류 | "이대로 가면 N개월 후 얼마?" — 구 잔소리봇 종료 |
| 뱅크샐러드 | 은행·카드 통합, 구독 탐지, 분류 수정 UI | 구독 해지 시 자산 궤적 변화 계산 없음; 분류 오류 직접 수정 부담 |
| Cleo AI | 채팅형 인사이트 ("왜 초과했어?") | 패턴 묘사까지만; 시간대·버킷·미래 궤적 연결 없음 |
| Monarch/Copilot | 가맹점별 그룹핑, MoM 비교 | 인과 서사 아님 |
| Rocket Money | 구독 탐지 + 해지 대행 | 유료 프리미엄; 국내 아님 |
| 온통청년 | 정책 리스트 + 조건 필터 | 개인 소비 패턴과 정책 자산 연결 없음 |
| KB·신한 적금 계산기 | 단일 상품 FV 계산 | 여러 상품+정책+소비 패턴 통합 없음 |

공통점: 전부 회고(retrospective). 미래(prospective) 시나리오 비교는 시장 공백.

---

## 4. 개념적 솔루션

| 솔루션 | 푸는 문제 | 차별점 |
|---|---|---|
| **B1. AI 지출 케어 코파일럿** (수집→진단→행동 루프) | P1, P3 | 수집 인프라는 이미 있음. 부족한 건 인과 서사 + 미래 궤적 |
| **B2. 불규칙 소득 적응형 버킷 엔진** | P4 | 고정예산 대신 선할당. AI가 배분율 계산 |
| **B3. 정책·예적금 결합 미래 자산 시뮬레이터** | P5 | Rocket Money는 유료 프리미엄, 온통청년은 목록만. 정책 매칭+현금흐름 통합 = 국내외 공백 |
| **B4. 미래 궤적 + 인과 서사 엔진** | P1, P3 | F4("줄이세요") 명령 대신 delta 시각화 → 이해 기반 행동 |

---

## 5. 핵심 기능

### 5-1. 기능 목록

| 기능 | 사용자 화면 | 푸는 문제 | 행동 메커니즘 |
|---|---|---|---|
| **F1. 거래내역 수집·정규화** | CSV 업로드 or 오픈뱅킹 테스트베드 데모 | P2, P4 | 수기 입력 제거 |
| **F2. LLM 자동 분류 + 교정 학습** | 가맹점명 → 카테고리 | P1, P2 | rule-only 취약 보완 |
| **F3. 이상 변화 탐지** | "배달 4→8회, 금토 심야 집중" | P2, P3 | C1 인과 서사의 트리거 |
| **C1. 인과 서사 생성** ⭐ | "배달 4회→8회 증가, 금토 심야, 여가 버킷 초과" — 한 문장 | P1, P3 | 시간·빈도·버킷 연결 → 이해 |
| **C2. 현재 패턴 미래 궤적** ⭐ | "현재 유지 시 6개월 후 320만" | P1 | 행동을 미래 결과로 번역 |
| **C3. 정책·예적금 결합** ⭐ | "도약계좌 조건 충족 시 만기 예상 X원" | P5 | 정책을 현금흐름에 반영 |
| **C4. 시나리오 delta 비교** ⭐ | "구독 해지 +38, 배달 감소 +82, 도약계좌 +192" | P1, P5 | 사용자가 비교 후 스스로 선택 |
| **C5. if-then 계획** | "IF 금토 심야 배달 THEN 마트 픽업 대체" | P1, P4 | Gollwitzer d=0.65(A): 구체 계획만 효과 |
| **C6. 결과 피드백** | "지난주 실행으로 +18K" | P1 | 학습 루프 |

⭐ = 기존 서비스에 없는 차별화 기능

### 5-2. 삭제한 기능

| 삭제 | 이유 |
|---|---|
| ~~F4 NBA 카드~~ ("이거 해보세요") | 명령이지 이해가 아님. C1-C5가 더 강하게 수행 |

### 5-3. 화면 예시

```text
현재 패턴 유지                  ████████░░░░░░░░░░░░  6개월 후 320만
  └ 원인: 배달 금토 심야 4회↑, 구독 5건 방치

시나리오 A (구독 2개 해지):        ██████████░░░░░░░░░░  358만 (+38)
시나리오 B (A + 배달 주1회↓):      ████████████░░░░░░░░  402만 (+82)
시나리오 C (B + 도약계좌 가입):    ████████████████████  512만 (+192)

IF 금·토 심야 배달 앱을 열면
THEN 이번 주에는 마트 픽업으로 대체
```

우리는 "줄이세요"라고 말하지 않는다. 사용자가 숫자 차이를 보고 스스로 결론을 낸다.

---

## 6. 구현 기술

| 기능 | 선택 기술 | 왜 / 대안 배제 이유 | 난이도 |
|---|---|---|---|
| F1 수집 | 카드사 CSV 파서(Pandas) + KFTC 오픈뱅킹 테스트베드 | 실계좌 운영 API는 이용기관 승인 필요(A) → 5주 밖. CSV는 법적 마찰 최소. 스크래핑/SMS 파싱은 약관·법 리스크 | 낮음 |
| F2 분류 | LLM structured output(function calling) + few-shot | rule-only는 신규 가맹점 취약, fine-tuning은 데이터 부족 | 낮음-중간 |
| F3 이상 탐지 | sklearn IsolationForest | 공식 문서·예제 풍부(A). Prophet은 유지보수 모드(v1.4.0, A). 딥러닝은 5주 내 과함 | 낮음 |
| C1 인과 서사 | Pandas 집계(빈도·시간대·버킷 잔액) + LLM structured output | 통계 feature가 사실 근거 강제 → hallucination 방지. rule-only는 서사 불가 | 중간 |
| C2+C4 궤적 | numpy-financial fv() — deterministic | 검증 완료(월 70만 × 60 @ 3.6% = 45,942,121원). Monte Carlo는 투자용 — 예적금엔 오버엔지니어링. ARIMA는 baseline엔 과함(월평균×N 충분) | 낮음 |
| C3 정책 매칭 | 공공 API(온통청년) + rule engine | 정책 자격은 결정론적 판정이 안전. LLM은 출처 기반 설명만 | 중간 |
| C5 if-then | Template engine(안전 경계) + LLM rewriting(멘탈 라벨 번역) | mental accounting(A): 추상 카테고리는 멘탈 모델과 안 맞아 행동 안 됨 | 낮음 |
| 시각화 | Recharts(React) 또는 fl_chart(Flutter) | baseline+scenario 겹치기 + 슬라이더 = 기본 기능 | 낮음 |
| DB/BE | PostgreSQL + FastAPI | 표준 RDB 충분. pgvector는 stretch | 낮음 |

### 기술 원칙

> LLM은 "설명"만 하고 "판단"은 하지 않게 한다.
> 자산 계산(fv())·정책 자격(rule engine)·이상 탐지(IsolationForest)는 결정론적.
> LLM은 계산 결과를 사람이 이해할 문장으로 번역할 뿐.

---

## 7. 6인 · 5주 구현 계획

### 7-1. 주차별

| 주차 | 목표 | 산출물 | 담당 분할(6인) |
|---|---|---|---|
| W1 | 거래 스키마, CSV 파서(신한·KB·삼성 3종), 더미 데이터, 정책 데이터 구조 | 업로드 → 정규화된 트랜잭션 배열 | BE 2(파서+API), FE 1(업로드 UI), PM 1(스키마·더미), DS 1(정책 데이터), DE 1(디자인) |
| W2 | LLM 분류 파이프라인(function calling), 교정 UI, 버킷 CRUD | 가맹점 → 카테고리 + 버킷 잔액 | BE 1(LLM), FE 1(교정 UI), BE 1(버킷 API), PM 1(테스트), DS 1(버킷 UX), DE 1(차트 준비) |
| W3 | 인과 feature 추출, baseline projection(fv()), 인과 서사 v1 | "배달 4→8회" + "N개월 후 X원" | DS 2(feature extraction), BE 1(fv API), FE 1(차트), PM 1(프롬프트), DE 1(UI 폴리싱) |
| W4 | 온통청년 API + 정책 rule engine, 슬라이더 + delta, IsolationForest | 정책 포함 미래 궤적 비교 UI | BE 1(정책), BE 1(delta), DS 1(IsoForest), FE 1(슬라이더), PM 1(프롬프트), DE 1(QA) |
| W5 | if-then 저장·피드백, E2E QA, 발표 자료 | 완성 데모 | 전원(QA + 발표) |

### 7-2. 우선순위

| 범위 | 포함 |
|---|---|
| **필수(MVP)** | CSV 입력, LLM 분류, 인과 서사 1종, fv() 궤적, 시나리오 delta, if-then |
| **가능하면** | 지자체 정책 1종, 구독 자동 탐지, IsolationForest, 오픈뱅킹 테스트베드 데모 |
| **제외** | 실계좌 운영 연동, 스크래핑, 자동 상품 가입, 투자수익률 예측, Monte Carlo |

### 7-3. 리스크

| 리스크 | 확률 | 대응 |
|---|---|---|
| 온통청년 API 응답 필드 상이 | 중 | W1 샘플 호출 확인, fallback 하드코딩 |
| 카드사 CSV 컬럼 상이 | 높음 | W1 3종 파서 먼저, 실패 시 붙여넣기 입력 |
| LLM 스키마 이탈 | 중 | 재시도 + JSON schema 강제 + rule fallback |
| 5주 내 기능 과다 | 높음 | MVP 엄격 적용, "가능하면"은 W4 go/no-go |

---

## 8. 기존 서비스와 차별화 요약

| 기준 | 기존 앱·정책 포털 | Setup |
|---|---|---|
| 방향 | 과거 회고 | **미래 예측(prospective)** |
| 출력 | "외식 23만 원" | "이 패턴 유지 시 6개월 후 320만" |
| 개입 | 사후 알림("줄이세요") | **사전 시나리오 비교**(delta) |
| 정책 연결 | 목록만 | **지원금·매칭이 궤적에 직접 반영** |
| 행동 유도 | 명령(nudge) | **이해(if-then + loss aversion)** |
| 설명 | 통계 요약 | **원인 → 변화 → 미래 결과 서사** |
| 안전성 | 상품 가입 유도 | 계산 가정·출처·기준일 표시 |

---

## 9. 검증 지표

| 검증 | 측정 | 목표 |
|---|---|---|
| 이해 | "왜 미래 자산이 달라졌는지" 설명 가능 | > 70% |
| 인과성 | 소비 변화와 delta 계산 일치 | 오차 < 1% |
| 선택 | 최소 1개 시나리오 비교·선택 | > 50% |
| 실행 | if-then 계획 저장 | > 30% |
| 지속성 | 4주 후 계획+지출 확인 | > 40% |
| 신뢰 | 정책 출처·기준일 확인 | > 60% |

---

## 최종 설득 문장

> **"Cleo가 말해주는 건 '네가 얼마나 썼는가'다. 토스가 보여주는 건 '네가 뭘 했는가'다. Setup이 말해주는 건 '네가 왜 그렇게 됐고, 무엇을 바꾸면 6개월 후 얼마가 달라지는가'다."
>
> **행동과학은 94개 실험에서 if-then 구체 계획만 행동 변화 효과가 있다고 증명했다(d=0.65). Setup은 그 계획을 사용자가 스스로 선택하게 만든다."
