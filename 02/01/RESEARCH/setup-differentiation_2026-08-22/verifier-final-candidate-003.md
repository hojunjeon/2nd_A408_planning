# CANDIDATE-003 최종 독립 검증

- verifier: `verifier-final-candidate-003`
- checked_at: 2026-08-22 KST
- status: **PASS**
- decision: A-01, A-03, A-04, T-07이 모두 충족된다.

## Findings by severity

### BLOCKER

- 없음.

### MAJOR

- 없음.

### MINOR

- 없음.

## 확인 근거

1. **원본 12개 산출물 보존**: `PLAN.md:23,50,73,94,122,137,151,170,212,229,253,276`에 원본 지시의 1~12번 산출물이 순서대로 각각 한 번 존재한다. 이는 `pasted-text.txt:306-320`과 일치한다. CANDIDATE-003은 기존 구조를 삭제하지 않고 `PLAN.md:333-493`에 보완 설계로 추가됐다.
2. **사용자 비판에 대한 실질적 보완**: `PLAN.md:8-19`는 기존 설계가 문서 추출·규칙 판정에 머물러 기술적 한 방이 약했다는 비판을 직접 수용한다. 보완안은 요약·검색이 아니라 Policy IR 컴파일, 반사실 상태 실행, 적대적 감사를 핵심으로 둔다(`PLAN.md:333-381`). 작은 정책 집합은 규칙만으로 처리할 수 있음을 인정하면서도, 이질적 공고 번역·자연어 이벤트 구조화·반례 생성이라는 AI 병목을 명확히 한 점도 타당하다(`PLAN.md:397-408`).
3. **세 히어로 AI의 사용자 가치와 기술 구체성**: Policy Compiler는 원문 위치·버전까지 보존하는 IR 필드와 검증 게이트를 제시한다(`PLAN.md:339-353`). Personal Policy Digital Twin은 취업·이사·소득 기준일·반려 후 보완을 사용자가 실행하는 가상 이벤트와 상태 delta로 연결한다(`PLAN.md:355-366`). Adversarial Policy Audit은 컴파일러, 시나리오, 반례, 인용, symbolic runtime의 역할과 사용자에게 반환할 보류 결과를 분리한다(`PLAN.md:368-395`). 계층과 핵심 객체도 구현 가능한 수준으로 정의됐다(`PLAN.md:410-430`).
4. **AI와 결정론적 판단의 경계**: AI는 구조화·후보·반례·질문을 제안할 뿐, 스키마·타입·기간 검증, 규칙 실행, 상태 delta, 인용·버전·충돌 확인, 최종 보류는 결정론적 코드가 담당한다(`PLAN.md:399-406,432-439`). 검증을 통과하지 못한 IR은 시뮬레이터에 들어가지 않으며(`PLAN.md:353`), 운영기관의 법적 판단·신청·지급·환수는 범위 밖이다(`PLAN.md:489`). 따라서 AI 역할은 의미 있지만 최종 자격·법적 판단을 생성 모델에 맡기지 않는다.
5. **연구 출처와 참고문헌 완결성**: CANDIDATE-003의 기술 근거는 Rules as Code 원 연구 보고서, arXiv 원 논문, ACL Anthology 공식 논문 페이지, PMLR 공식 ICML 논문 페이지다(`hero-ai-research.md:18-29`; `PLAN.md:381,464`). 직접 열어 제목·저자/기관·출판처 또는 원 논문 상태를 재확인했다. 본문 전체의 고유 외부 URL 42개와 `PLAN.md:280-327`의 참고문헌 URL 42개가 정확히 일치하며 누락·중복이 없다. 새 근거 8건도 `PLAN.md:320-327`에 모두 포함된다. arXiv 자료는 원 논문이지만 출판 심사 완료를 뜻하지 않으며, 문서도 논문 수치를 Setup 성능으로 전용하지 않는다(`PLAN.md:381,488`).
6. **실패 시험과 STARR**: C3-01~C3-08은 표 예외, 버전 충돌, 문법상 유효하지만 의미상 틀린 IR, 미래 이벤트, 상충 공고, 날짜 불일치, 누락 입력, 잘못된 감사 반례를 의도적으로 재현한다(`PLAN.md:441-452`). STARR-3~5는 각각 의미 오류, 현재 상태에 머문 실패, 그럴듯한 오답의 반대심문을 Situation→Task→Action→Result→Reflection으로 추적하며(`PLAN.md:456-480`), 모든 결과 수치는 구현 후 로그로만 채우고 현재는 UNVERIFIED다(`PLAN.md:454,463,471,479,482`). 단순 규칙 추가가 아닌 재현 가능한 트러블슈팅 서사다.
7. **제공자·개인정보·미검증 경계·일정 제외**: 제공자는 경쟁사가 아니라 원문·데이터·신청·상담 연결원으로 유지된다(`PLAN.md:27-30,73-92`). 금융 로그인·스크래핑·실계좌·SMS 자동수집·자동 신청·자격 확정은 제외하고 최소 입력과 비식별 합성 데이터를 사용한다(`PLAN.md:30,192-197`). `UNVERIFIED/BLOCKED` 정의와 실패 처리도 유지된다(`PLAN.md:42-48,199-209,253-274,484-493`). 구현·실측 결과·세부 일정표는 없고(`PLAN.md:6`), `hero-ai-research.md:42-44`의 7주 언급은 주차별 일정이 아니라 증명 가능한 최소 세로 슬라이스 범위다.

## Acceptance

| 항목 | 판정 | 근거 |
|---|---|---|
| A-01 | PASS | 커뮤니티, 영상/SNS, 대안 행동, 공식 제공자 출처군이 보존됐고 제공자 역할과 차단 경계가 유지됐다(`PLAN.md:23-92`). |
| A-03 | PASS | 선택 방향의 Policy Compiler, Personal Policy Digital Twin, Adversarial Policy Audit에 직접 원 연구/공식 출판 근거, 필요한 데이터·객체, 실패 조건, 비교 시험이 있다(`PLAN.md:333-454`; `hero-ai-research.md:18-40`). |
| A-04 | PASS | 12개 요청 섹션이 모두 존재하고 CANDIDATE-003 보완 이후에도 `UNVERIFIED/BLOCKED`, 개인정보, 제공자, 최종 판단 경계가 보존됐다(`PLAN.md:23-327,333-493`). |
| T-07 | PASS | 12개 출력, 직접 링크 42개 전수 수록, 명시적 미검증/차단, 결과 비조작, 구현·세부 일정 제외 조건을 모두 충족한다. |

**최종 결정: PASS.** CANDIDATE-003은 단순 RAG·단순 규칙·알림을 넘어 실행 가능한 정책 프로그램, 반사실 상태 변화, 적대적 반례 검증을 사용자 흐름으로 연결했고, 생성 AI의 결과는 결정론적 검증과 운영기관 최종 판단 경계 안에 남겼다. A-05 동결 매니페스트와 `TASK.md`/`state.json` 상태 갱신은 이 읽기 전용 검증 이후의 후속 단계다.
